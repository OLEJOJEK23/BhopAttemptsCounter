name: tryes_counter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  drift: ^2.20.0
  drift_flutter: ^0.2.0
  
  cupertino_icons: ^1.0.8

  win32: ^5.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  drift_dev: ^2.20.0
  build_runner: ^2.4.11
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
